import*as THREE from'three';import{OrbitControls}from'three/addons/controls/OrbitControls.js';let scene,camera,renderer,controls,group,wire=false;export function inizializzaScena3D(){const b=document.getElementById('threeView');scene=new THREE.Scene();scene.background=new THREE.Color(0x0d131a);camera=new THREE.PerspectiveCamera(45,1,.1,1000);renderer=new THREE.WebGLRenderer({antialias:true});renderer.setPixelRatio(Math.min(2,devicePixelRatio));b.appendChild(renderer.domElement);controls=new OrbitControls(camera,renderer.domElement);controls.enableDamping=true;scene.add(new THREE.HemisphereLight(0xffffff,0x334455,1.7));const l=new THREE.DirectionalLight(0xffffff,1);l.position.set(20,30,20);scene.add(l);scene.add(new THREE.GridHelper(100,100,0x304050,0x18222d));group=new THREE.Group();scene.add(group);resizeScena3D();animate()}function animate(){requestAnimationFrame(animate);controls?.update();if(renderer)renderer.render(scene,camera)}function box(x,y,z,w,d,h,mat=0x87929d){const m=new THREE.Mesh(new THREE.BoxGeometry(w,d,h),new THREE.MeshStandardMaterial({color:mat,wireframe:wire}));m.position.set(x,y,z);group.add(m)}export function aggiornaScena3D(s){if(!group)return;while(group.children.length)group.remove(group.children[0]);const L=s.campate.reduce((a,c)=>a+c.interasse,0),W=s.generale.luce,H=s.generale.altezzaPilastro,top=H+W*s.generale.pendenzaCopertura/200;let x=0;const xs=[0];s.campate.forEach(c=>{x+=c.interasse;xs.push(x)});xs.forEach(px=>{box(px,0,H/2,.4,.4,H);box(px,W,H/2,.4,.4,H)});box(L/2,0,H+.3,L,.5,.6);box(L/2,W,H+.3,L,.5,.6);xs.slice(0,-1).forEach((px,i)=>{const span=s.campate[i],mid=px+span/2,dy=W/2,dz=top-H,len=Math.hypot(dy,dz),a=Math.atan2(dz,dy);const m=new THREE.Mesh(new THREE.BoxGeometry(span,.35,.55),new THREE.MeshStandardMaterial({color:0x9ca6af,wireframe:wire}));m.position.set(mid,W/2,H+.3+dz/2);m.rotation.x= -a;group.add(m)});const pm=new THREE.MeshStandardMaterial({color:0x607080,transparent:true,opacity:.55,wireframe:wire});box(L/2,-.12,H/2,L,.2,H,pm.color);box(L/2,W+.12,H/2,L,.2,H,pm.color);box(-.12,W/2,H/2,.2,W,H,pm.color);box(L+.12,W/2,H/2,.2,W,H,pm.color);fit(s)}function fit(s){const L=s.campate.reduce((a,c)=>a+c.interasse,0),W=s.generale.luce,H=s.generale.altezzaPilastro;camera.position.set(L*.9,-W*1.2,H*.9);controls.target.set(L/2,W/2,H/2);controls.update()}export function vistaIsometrica(){controls.target.set(0,0,0);camera.position.set(30,-35,25);controls.update()}export function vistaTop(){camera.position.set(0,0,60);controls.target.set(10,10,0);controls.update()}export function vistaFront(){camera.position.set(20,-60,10);controls.target.set(10,0,5);controls.update()}export function vistaLato(){camera.position.set(60,0,10);controls.target.set(10,10,5);controls.update()}export function toggleWireframe(){wire=!wire;aggiornaScena3D(window.CACEM_STATE)}export function resizeScena3D(){const b=document.getElementById('threeView');if(!renderer)return;const w=b.clientWidth,h=b.clientHeight;renderer.setSize(w,h);camera.aspect=w/h;camera.updateProjectionMatrix()}