const nomi=['pilastri','travi','mensole','solai','copertura','coppelle','pannelli','listino'];export let catalogo={};export async function caricaCataloghi(){for(const n of nomi){const r=await fetch(n+'.json');if(!r.ok)throw new Error('Catalogo non trovato: '+n);catalogo[n]=await r.json()}return catalogo}export function getCatalogo(n){return catalogo[n]||[]}const by=(n,id)=>(catalogo[n]||[]).find(x=>x.id===id);export const trovaPilastro=id=>by('pilastri',id);export const trovaTrave=id=>by('travi',id);export const trovaTegolo=id=>by('copertura',id);export const trovaSolaio=id=>by('solai',id);export const trovaMensola=id=>by('mensole',id);export const trovaPannello=id=>(catalogo.pannelli?.tipi||[]).find(x=>x.id===id)||null;